import React from "react";
import {
  Table as TableLibrary,
  Header,
  HeaderRow,
  HeaderCell,
  Body,
  Row,
  Cell,
} from "@table-library/react-table-library/table";
import {
  useSort,
  HeaderCellSort,
} from "@table-library/react-table-library/sort";

const EmpresaTableByYear = ({ metricsByYear, empresas }) => {
  console.log(metricsByYear);

  return <div></div>;
};

export default EmpresaTableByYear;
